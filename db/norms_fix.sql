-- Возврат нормы паратгормона к пг/мл.
-- Раньше диапазон переводили в пмоль/л под конкретный файл, но лаборатории отчитываются
-- в разных единицах, и одна глобальная единица на показатель не работает.
-- Теперь единица читается из заголовка колонки и значение пересчитывается при загрузке
-- (services/reports/units.js), поэтому справочник хранит общепринятые пг/мл.
-- Выполнять только если ранее применяли перевод в пмоль/л.

UPDATE analysis_norms
SET min_value = 15.0, max_value = 65.0, unit = 'пг/мл'
WHERE name IN ('Паратгормон', 'Паратгормон после операции');
